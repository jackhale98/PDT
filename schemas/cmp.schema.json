{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pdt.dev/schemas/component.schema.json",
  "title": "Component",
  "description": "An individual part (purchased or manufactured)",
  "type": "object",
  "required": ["id", "part_number", "title", "make_buy", "category", "status", "created", "author"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^CMP-[0-9A-Z]{26}$",
      "description": "Unique identifier (CMP prefix + ULID)"
    },
    "part_number": {
      "type": "string",
      "minLength": 1,
      "maxLength": 100,
      "description": "Part number"
    },
    "revision": {
      "type": ["string", "null"],
      "maxLength": 20,
      "description": "Part revision"
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200,
      "description": "Short descriptive title"
    },
    "description": {
      "type": ["string", "null"],
      "description": "Detailed description"
    },
    "make_buy": {
      "type": "string",
      "enum": ["make", "buy"],
      "default": "buy",
      "description": "Make or buy decision"
    },
    "category": {
      "type": "string",
      "enum": ["mechanical", "electrical", "software", "fastener", "consumable"],
      "default": "mechanical",
      "description": "Component category"
    },
    "material": {
      "type": ["string", "null"],
      "description": "Material specification"
    },
    "mass_kg": {
      "type": ["number", "null"],
      "minimum": 0,
      "description": "Mass in kilograms"
    },
    "unit_cost": {
      "type": ["number", "null"],
      "minimum": 0,
      "description": "Unit cost"
    },
    "selected_quote": {
      "type": ["string", "null"],
      "description": "ID of the selected quote for this component"
    },
    "suppliers": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "supplier_id": {
            "type": ["string", "null"],
            "description": "Supplier entity ID (SUP-...) - links to SUP entity"
          },
          "name": {
            "type": ["string", "null"],
            "description": "Supplier name (fallback if supplier_id not set)"
          },
          "supplier_pn": {
            "type": ["string", "null"],
            "description": "Supplier's part number"
          },
          "lead_time_days": {
            "type": ["integer", "null"],
            "minimum": 0,
            "description": "Lead time in days"
          },
          "moq": {
            "type": ["integer", "null"],
            "minimum": 1,
            "description": "Minimum order quantity"
          },
          "unit_cost": {
            "type": ["number", "null"],
            "minimum": 0,
            "description": "Unit cost from this supplier"
          }
        }
      },
      "description": "Supplier information (use supplier_id to link to SUP entities)"
    },
    "documents": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["type", "path"],
        "properties": {
          "type": {
            "type": "string",
            "description": "Document type (drawing, specification, datasheet, etc.)"
          },
          "path": {
            "type": "string",
            "description": "Path to the document"
          },
          "revision": {
            "type": ["string", "null"],
            "description": "Document revision"
          }
        }
      },
      "description": "Associated documents"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Tags for filtering and organization"
    },
    "status": {
      "type": "string",
      "enum": ["draft", "review", "approved", "released", "obsolete"],
      "default": "draft",
      "description": "Current status"
    },
    "links": {
      "type": "object",
      "properties": {
        "requirements": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "IDs of requirements this component satisfies"
        },
        "processes": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "IDs of processes used to manufacture this component"
        },
        "tests": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "IDs of tests for this component"
        },
        "risks": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "IDs of risks affecting this component"
        },
        "used_in": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "IDs of assemblies using this component"
        },
        "replaces": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "IDs of components this replaces"
        },
        "replaced_by": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "IDs of components that replace this"
        },
        "interchangeable_with": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "IDs of interchangeable components"
        },
        "related_to": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "IDs of related entities"
        }
      }
    },
    "created": {
      "type": "string",
      "format": "date-time",
      "description": "Creation timestamp"
    },
    "author": {
      "type": "string",
      "description": "Author name"
    },
    "entity_revision": {
      "type": "integer",
      "minimum": 1,
      "default": 1,
      "description": "Entity revision number"
    }
  },
  "additionalProperties": true
}
